- name: Configure all lab containers
  hosts: containers
  become: true
  gather_facts: true

  roles:
    - role: common
      tags: common

    - role: users
      tags: users

    - role: nginx
      tags: nginx

    - role: firewall
      tags: firewall
