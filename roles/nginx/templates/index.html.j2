<!doctype html>
<html>
  <head>
    <title>{{ nginx_site_title }}</title>
  </head>
  <body>
    <h1>{{ nginx_site_title }}</h1>
    <p>This page was deployed by Ansible.</p>
    <p>Host: {{ inventory_hostname }}</p>
    <p>OS family: {{ ansible_os_family }}</p>

    <p>Secret: {{ secret_message }}</p>
  </body>
</html>